{% extends 'base.html.twig' %}
{% block content %}
{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
     <div class="row">
    <div class="col">{{ form_row(form.username) }}</div>
</div>

{{ form_rest(form) }}

<a href="{{ path('praticien.add') }}">Create an account</a>
{% endblock %}

{% block title %}
Praticien </br>
{% endblock %}
{% block link %} 
<a href="{{ path('patient.add') }}">Connexion Patient</a>
{% endblock %}